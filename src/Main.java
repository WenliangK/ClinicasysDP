import ClinicaUtil.ConexionBD;public class git config --global user.name "TU NOMBRE DE USUARIO"Main {    public static void main(String[] args) {        // Obtenemos la instancia        ConexionBD db1 = ConexionBD.getInstancia();        db1.ejecutarConsulta("SELECT * FROM pacientes");        // Intentamos obtener otra instancia        ConexionBD db2 = ConexionBD.getInstancia();        db2.ejecutarConsulta("SELECT * FROM citas");        // Verificamos si son la misma instancia        if (db1 == db2) {            System.out.println("¡Singleton exitoso! Ambas variables apuntan al mismo objeto.");        }    }}